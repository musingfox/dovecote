name: Release CLI

on:
  push:
    tags:
      - "cli-v*"

permissions:
  contents: write

jobs:
  build:
    name: Build ${{ matrix.target }}
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 20
    strategy:
      fail-fast: true
      matrix:
        include:
          - target: bun-linux-x64
            runner: ubuntu-latest
            archive: tar.gz
            binary: dovecote
          - target: bun-linux-arm64
            runner: ubuntu-latest
            archive: tar.gz
            binary: dovecote
          - target: bun-darwin-x64
            runner: macos-latest
            archive: tar.gz
            binary: dovecote
          - target: bun-darwin-arm64
            runner: macos-latest
            archive: tar.gz
            binary: dovecote
          - target: bun-windows-x64
            runner: ubuntu-latest
            archive: zip
            binary: dovecote.exe
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Build binary (${{ matrix.target }})
        working-directory: cli
        run: |
          mkdir -p dist
          bun build --compile --target=${{ matrix.target }} src/cli.ts --outfile dist/${{ matrix.binary }}

      - name: Package archive
        working-directory: cli/dist
        run: |
          ASSET="dovecote-${{ matrix.target }}.${{ matrix.archive }}"
          if [ "${{ matrix.archive }}" = "zip" ]; then
            zip "$ASSET" "${{ matrix.binary }}"
          else
            tar -czf "$ASSET" "${{ matrix.binary }}"
          fi
          ls -la

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: dovecote-${{ matrix.target }}
          path: cli/dist/dovecote-${{ matrix.target }}.${{ matrix.archive }}
          retention-days: 1

  release:
    name: Publish GitHub Release
    needs: build
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts/

      - name: Collect archives
        run: |
          mkdir -p release
          find artifacts -type f \( -name "*.tar.gz" -o -name "*.zip" \) -exec cp {} release/ \;
          ls -la release/

      - name: Compute SHA256SUMS
        working-directory: release
        run: |
          sha256sum *.tar.gz *.zip 2>/dev/null | sort > SHA256SUMS
          cat SHA256SUMS

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            release/*.tar.gz
            release/*.zip
            release/SHA256SUMS
          fail_on_unmatched_files: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
